# =============================================
# ATERA PHỐ NỐI - Production Environment
# =============================================
# Copy this file to .env and update values

# Database (Neon PostgreSQL)
DATABASE_URL=jdbc:postgresql://your-neon-host/neondb?sslmode=require
DATABASE_USERNAME=your_username
DATABASE_PASSWORD=your_password

# JWT Secret (generate a strong secret for production)
JWT_SECRET=your-very-long-secret-key-at-least-256-bits

# CORS Origins (your domain)
CORS_ORIGINS=https://yourdomain.com,https://www.yourdomain.com

# Frontend API URL (use /api/v1 when behind Nginx proxy)
VITE_API_URL=/api/v1

# Email Configuration (Gmail example)
MAIL_HOST=smtp.gmail.com
MAIL_PORT=587
MAIL_USERNAME=your-email@gmail.com
MAIL_PASSWORD=your-app-password
ADMIN_EMAIL=admin@atera.com
